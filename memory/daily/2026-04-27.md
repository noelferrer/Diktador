---
type: memory-daily
date: 2026-04-27
---

# 2026-04-27

Mid-PR pause. Single feature in flight: bare-Fn push-to-talk with Input Monitoring permission flow. Plan executed cleanly through Phase G; stopped before computer-use verification.

## What landed (on `feat/hotkey-fn-trigger`, not yet pushed)

14 commits ahead of `main`. Spec + plan + 6 code phases + 6 docs phases + 1 review fixup.

- **Code (Phases B–F)** — `HotkeyRegistry` extended with a parallel `register(modifierTrigger:)` path over `NSEvent.addGlobalMonitorForEvents` + paired local monitor. New public types `ModifierTrigger` and `InputMonitoringStatus`. New permission accessors (`inputMonitoringPermission`, `requestInputMonitoringPermission(completion:)`). Internal `PermissionProvider` test seam. `AppDelegate` rewired to a 3-state bootstrap (granted → register Fn; undetermined → request + recurse; denied → warning UI + deep-link to System Settings). 8/8 XCTest cases pass; `xcodebuild` Debug + Release green.
- **Docs (Phase G)** — module README updated; `memory/domains/hotkey.md` updated (Fn open-question closed; new `IOHIDAccessType default:` blind-spot note); new ADR `wiki/decisions/hotkey-modifier-only-trigger.md`; new how-to `wiki/howtos/first-run-setup.md` (Press 🌐 to: Do nothing + Input Monitoring grant); `wiki/index.md` Decisions 1→2 and Howtos 0→1; two log entries with placeholders for PR URL and /simplify summary.

## Subagent-driven-development cadence (worked well)

- Implementer (sonnet) → spec reviewer (haiku) → code-quality reviewer (`superpowers:code-reviewer`, opus). One implementer per plan phase.
- Spec reviewer caught zero meaningful regressions across six dispatches — the implementers held to the plan's verbatim code blocks.
- Code-quality reviewer caught three documentation issues in Phase G (async-permission threading clarity; "Input Monitoring denied" failure mode missed local-monitor pairing; ADR Decision section missed local monitor) — all fixed in commit `5da1218` before the next phase. Earlier phases produced only minor nits, all explicitly defer-friendly.
- No BLOCKED or NEEDS_CONTEXT escalations across the whole branch.

## Decisions ratified this session

- **Bare-Fn over Option+Space** as v1 push-to-talk default. Option+Space dropped (settings module reintroduces user choice later).
- **Dual-path inside one module** (Carbon for `KeyCombo`, NSEvent for `ModifierTrigger`) over splitting into two modules. Same `RegistrationToken` type spans both paths.
- **Permission UX**: registry exposes status + async request; `AppDelegate` orchestrates the user-facing flow; denied state is a non-functional warning UI with a deep-link, not an auto-fallback to a different combo.
- **Right-side modifiers deferred** to a separate PR — same NSEvent infrastructure, but a different API surface (sided variant of `KeyCombo.modifiers`, not a new `ModifierTrigger` case).

## Things that bit / Open questions

- Phase G code reviewer flagged the local-monitor pairing as missing from both the README failure-mode bullet and the ADR Decision section. Lesson: when the implementer faithfully copies a spec verbatim, gaps in the spec/plan propagate; the code reviewer catches them. Folded into commit `5da1218`.
- The `IOHIDAccessType` `default:` branch in `IOHIDPermissionProvider.currentStatus()` is a latent blind spot if Apple adds a fourth case. Filed in `memory/domains/hotkey.md` open questions; not blocking.
- Phase D's verbatim file replacement dropped the doc comments from `register(combo:)` and `unregister(_:)`. Code reviewer caught it; restored in Phase E. Lesson for plans: when the plan includes a "replace this file with" block, double-check that documentation comments survive the copy.

## What's still pending (next session)

- **H2** — computer-use verification (granted-state, denied-state, globe-key sanity). User-driven. Detailed script in `memory/resume.md`.
- **H3** — `/simplify` pass.
- **H4** — `git push -u origin feat/hotkey-fn-trigger` + `gh pr create` + patch the two `<fill in…>` placeholders in `log.md` + final push.
- **H5** — rewrite `memory/resume.md` for the post-ship state.
- Final code review across all 14 commits.
